# Main Table Structure Reference

> **Generated by setup from `{{source_docx}}` on {{rendered_on}}.**
> Values below were extracted from that branded template. To change them, edit this
> file directly — setup will not overwrite it again. To re-derive everything from a
> different .docx, run `setup_docx_renderer.py <new.docx> --force`.

## Contents
- Table properties
- Header row
- Row model — one row per section
- Content row — col 1 (row number)
- Content row — col 2 (discussion content, list levels)
- Content row — col 3 (action)
- Content row — col 4 (status)
- Meetings row (final row)

---

## Table properties

Total width {{tbl_total_w}} DXA. Columns: {{col1_w}} / {{col2_w}} / {{col3_w}} / {{col4_w}}.

```xml
<w:tbl>
  <w:tblPr>
    <w:tblStyle w:val="TableGrid"/>
    <w:tblW w:w="{{tbl_total_w}}" w:type="dxa"/>
    <w:tblLook w:val="{{tbl_look_val}}" w:firstRow="{{tbl_look_first_row}}" w:lastRow="{{tbl_look_last_row}}"
               w:firstColumn="{{tbl_look_first_col}}" w:lastColumn="{{tbl_look_last_col}}"
               w:noHBand="{{tbl_look_no_hband}}" w:noVBand="{{tbl_look_no_vband}}"/>
  </w:tblPr>
  <w:tblGrid>
    <w:gridCol w:w="{{col1_w}}"/>
    <w:gridCol w:w="{{col2_w}}"/>
    <w:gridCol w:w="{{col3_w}}"/>
    <w:gridCol w:w="{{col4_w}}"/>
  </w:tblGrid>
  <!-- rows go here -->
</w:tbl>
```

| Col | Role | Alignment |
|-----|------|-----------|
| 1 | Auto row number | Centre |
| 2 | Discussion content | Left |
| 3 | Action (company name only) | Centre |
| 4 | Status | Centre |

---

## Header row

Row height {{header_row_h}} DXA. Col 1 is empty; cols 2–4 have bold centred text.

```xml
<w:tr>
  <w:trPr><w:trHeight w:val="{{header_row_h}}"/></w:trPr>
  <w:tc>
    <w:tcPr><w:tcW w:w="{{col1_w}}" w:type="dxa"/></w:tcPr>
    <w:p><w:pPr><w:spacing w:after="0"/><w:jc w:val="center"/></w:pPr></w:p>
  </w:tc>
  <w:tc>
    <w:tcPr><w:tcW w:w="{{col2_w}}" w:type="dxa"/></w:tcPr>
    <w:p>
      <w:pPr>
        <w:spacing w:after="0"/>
        <w:jc w:val="center"/>
        <w:rPr>
          <w:rFonts w:ascii="{{tbl_font_xml}}" w:hAnsi="{{tbl_font_xml}}"/>
          <w:b/><w:bCs/>
          <w:sz w:val="{{tbl_sz}}"/><w:szCs w:val="{{tbl_sz}}"/>
          <w:lang w:val="{{tbl_lang}}"/>
        </w:rPr>
      </w:pPr>
      <w:r>
        <w:rPr>
          <w:rFonts w:ascii="{{tbl_font_xml}}" w:hAnsi="{{tbl_font_xml}}"/>
          <w:b/><w:bCs/>
          <w:sz w:val="{{tbl_sz}}"/><w:szCs w:val="{{tbl_sz}}"/>
          <w:lang w:val="{{tbl_lang}}"/>
        </w:rPr>
        <w:t>{{project_name_xml}}</w:t>
      </w:r>
    </w:p>
  </w:tc>
  <!-- Repeat pattern for Action and Status columns -->
</w:tr>
```

---

## Row model — one row per section

**A table row is a SECTION, not an item.** One top-level section of the notes
occupies exactly one `<w:tr>`. Everything belonging to that section — its title,
its subsection labels and all of its numbered sub-items — lives as multiple
paragraphs inside that single row's col 2.

Do not open a new row for each sub-item. Col 1 carries the section counter, so a
row per sub-item advances the section number on every line: section titles lose
their number entirely and sub-items render with a zero in the middle ("3.0.1")
because the ilvl-1 counter never advanced. `validate.py` fails a document whose
rows advance the counter without carrying a section title.

```
<w:tr>                          ← section 1
  col 1: counter paragraph      ← numPr, ilvl 0, numId {{num_id}}  → renders "1."
  col 2: section title          ← numPr, ilvl 0, numId="0"    → number suppressed
         subsection label       ← numPr, ilvl 1, numId {{num_id}}  → renders "1.1"
         sub-item               ← numPr, ilvl 2, numId {{num_id}}  → renders "1.1.1"
         sub-item               ← numPr, ilvl 2, numId {{num_id}}  → renders "1.1.2"
  col 3: blank ×3, then "Org A" on the paragraph facing its sub-item
  col 4: blank ×3, then "Pending" on the same index
</w:tr>
<w:tr>                          ← section 2
  ...
</w:tr>
```

Cols 3 and 4 hold one paragraph per col-2 paragraph, so an Action or Status sits
on the same visual line as the sub-item it belongs to. Paragraphs facing a section
title or a subsection label are empty.

The final Meetings row is the one exception: it carries no counter in col 1.

---

## Content rows — shared border rule

Every content row cell (except the Meetings row) has a bottom border only:

```xml
<w:tcBorders>
  <w:bottom w:val="single" w:sz="4" w:space="0" w:color="auto"/>
</w:tcBorders>
```

---

## Col 1 — Row number (auto-list)

Style `{{col1_style}}`, numbering `numId="{{num_id}}"` from `word/numbering.xml`.
Reference it — never redefine it.

```xml
<w:tc>
  <w:tcPr>
    <w:tcW w:w="{{col1_w}}" w:type="dxa"/>
    <w:tcBorders>
      <w:bottom w:val="single" w:sz="4" w:space="0" w:color="auto"/>
    </w:tcBorders>
  </w:tcPr>
  <w:p>
    <w:pPr>
      <w:pStyle w:val="{{col1_style}}"/>
      <w:numPr>
        <w:ilvl w:val="0"/>
        <w:numId w:val="{{num_id}}"/>
      </w:numPr>
      <w:spacing w:line="240" w:lineRule="auto"/>
    </w:pPr>
  </w:p>
</w:tc>
```

---

## Col 2 — Content (list hierarchy)

All levels reference `numId="{{num_id}}"`. Do not redefine — reference only.

### Level 0 — Section title

The section's own number is painted by the col 1 counter, so this paragraph must
**suppress** its number rather than omit numbering altogether. `numId="0"` at
`ilvl="0"` is the suppression — it is deliberate here and is the one place in the
document where `numId="0"` is correct.

Omitting `<w:numPr>` entirely is a bug: the paragraph then falls back to its list
style's own numbering and the section title drifts out of the hierarchy.

```xml
<w:p>
  <w:pPr>
    <w:pStyle w:val="{{col2_style_l0}}"/>
    <w:numPr>
      <w:ilvl w:val="0"/>
      <w:numId w:val="0"/>
    </w:numPr>
    <w:spacing w:line="240" w:lineRule="auto"/>
  </w:pPr>
  <w:r>
    <w:t>Section title text</w:t>
  </w:r>
</w:p>
```

### Level 1 — Subsection label

Renders as "1.1", "1.2" … Italic, no bold. One per topic within the section.

```xml
<w:p>
  <w:pPr>
    <w:pStyle w:val="{{col2_style_l2}}"/>
    <w:numPr>
      <w:ilvl w:val="1"/>
      <w:numId w:val="{{num_id}}"/>
    </w:numPr>
    <w:spacing w:line="240" w:lineRule="auto"/>
  </w:pPr>
  <w:r><w:rPr><w:i/></w:rPr><w:t>Subsection label</w:t></w:r>
</w:p>
```

### Level 2 — Numbered sub-items

```xml
<w:p>
  <w:pPr>
    <w:pStyle w:val="{{col2_style_l2}}"/>
    <w:numPr>
      <w:ilvl w:val="2"/>
      <w:numId w:val="{{num_id}}"/>
    </w:numPr>
    <w:spacing w:after="240" w:line="240" w:lineRule="auto"/>
    <w:ind w:left="{{l2_left}}" w:hanging="{{l2_hanging}}"/>
  </w:pPr>
  <w:r><w:t>Sub-item text here.</w:t></w:r>
</w:p>
```

### Level 3 — Bullet sub-items

```xml
<w:p>
  <w:pPr>
    <w:pStyle w:val="{{col2_style_l3}}"/>
    <w:numPr>
      <w:ilvl w:val="3"/>
      <w:numId w:val="{{num_id}}"/>
    </w:numPr>
    <w:spacing w:after="240" w:line="240" w:lineRule="auto"/>
    <w:ind w:left="{{l3_left}}" w:hanging="{{l3_hanging}}"/>
  </w:pPr>
  <w:r><w:t>Bullet text here.</w:t></w:r>
</w:p>
```

---

## Col 3 — Action

**HARD RULE: Company or team names only. Never individual person names.**

Col 3 holds one paragraph per col-2 paragraph in the same row, so each Action
lands on the line of the sub-item it belongs to. Paragraphs facing the section
title, a subsection label, or a sub-item with no owner are empty.

```xml
<w:tc>
  <w:tcPr>
    <w:tcW w:w="{{col3_w}}" w:type="dxa"/>
    <w:tcBorders>
      <w:bottom w:val="single" w:sz="4" w:space="0" w:color="auto"/>
    </w:tcBorders>
  </w:tcPr>
  <w:p><w:pPr><w:spacing w:after="120"/></w:pPr></w:p>
  <w:p>
    <w:pPr>
      <w:spacing w:after="0"/>
      <w:jc w:val="center"/>
    </w:pPr>
    <w:r><w:t>[Organisation Name]</w:t></w:r>
  </w:p>
</w:tc>
```

---

## Col 4 — Status

Same structure as Col 3, width {{col4_w}}. Permitted values: `In Progress` / `Pending` / `No Action`. No other values.

---

## Meetings row (final table row)

Row height {{meetings_row_h}} DXA. No bottom border on any cell. Only col 2 has content — two bold paragraphs.

```xml
<w:tr>
  <w:trPr><w:trHeight w:val="{{meetings_row_h}}"/></w:trPr>
  <!-- Cols 1, 3, 4: empty paragraphs only -->
  <w:tc>
    <w:tcPr><w:tcW w:w="{{col2_w}}" w:type="dxa"/></w:tcPr>
    <w:p>
      <w:pPr><w:spacing w:after="0" w:line="240" w:lineRule="auto"/></w:pPr>
      <w:r><w:t>MEETINGS</w:t></w:r>
    </w:p>
    <w:p>
      <w:pPr><w:spacing w:after="0" w:line="240" w:lineRule="auto"/></w:pPr>
      <w:r><w:t>The next meeting date is [date, formatted as {{date_format}}].</w:t></w:r>
    </w:p>
  </w:tc>
</w:tr>
```
