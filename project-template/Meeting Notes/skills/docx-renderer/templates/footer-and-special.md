# Footer and Special Formatting Reference

> **Generated by setup from `{{source_docx}}` on {{rendered_on}}.**
> Values below were extracted from that branded template. To change them, edit this
> file directly — setup will not overwrite it again. To re-derive everything from a
> different .docx, run `setup_docx_renderer.py <new.docx> --force`.

## Contents
- Footer paragraph
- Superscript ordinal dates
- Special character escaping
- Common XML pitfalls

---

## Footer paragraph

Placed after `</w:tbl>`, before `<w:sectPr>`. Spacing before {{footer_spacing_before}}.

Organisation `{{org_name}}{{org_suffix}}` in bold, brand colour `#{{org_color}}`.

```xml
<w:p>
  <w:pPr>
    <w:spacing w:before="{{footer_spacing_before}}"/>
    <w:rPr>
      <w:rFonts w:ascii="{{footer_font_xml}}" w:hAnsi="{{footer_font_xml}}"/>
      <w:sz w:val="{{footer_sz}}"/><w:szCs w:val="{{footer_sz_cs}}"/>
      <w:lang w:val="{{footer_lang}}"/>
    </w:rPr>
  </w:pPr>
  <w:r>
    <w:rPr>
      <w:rFonts w:ascii="{{footer_font_xml}}" w:hAnsi="{{footer_font_xml}}"/>
      <w:sz w:val="{{footer_sz}}"/><w:szCs w:val="{{footer_sz_cs}}"/>
      <w:lang w:val="{{footer_lang}}"/>
    </w:rPr>
    <w:t xml:space="preserve">Prepared by: </w:t>
  </w:r>
  <w:r>
    <w:rPr>
      <w:rFonts w:ascii="{{footer_font_xml}}" w:hAnsi="{{footer_font_xml}}"/>
      <w:b/><w:bCs/>
      <w:color w:val="{{org_color}}"/>
      <w:sz w:val="{{footer_sz}}"/><w:szCs w:val="{{footer_sz_cs}}"/>
      <w:lang w:val="{{footer_lang}}"/>
    </w:rPr>
    <w:t>{{org_name_xml}}</w:t>
  </w:r>
  <w:r>
    <w:rPr>
      <w:rFonts w:ascii="{{footer_font_xml}}" w:hAnsi="{{footer_font_xml}}"/>
      <w:sz w:val="{{footer_sz}}"/><w:szCs w:val="{{footer_sz_cs}}"/>
      <w:lang w:val="{{footer_lang}}"/>
    </w:rPr>
    <w:t xml:space="preserve">{{org_suffix_xml}}</w:t>
  </w:r>
</w:p>
```

{{org_color_warning}}

---

## Superscript ordinal dates

When a date includes an ordinal suffix (1st, 2nd, 19th, etc.), split into three runs:

```xml
<w:r>
  <w:rPr><w:lang w:val="{{footer_lang}}"/></w:rPr>
  <w:t xml:space="preserve">June 19</w:t>
</w:r>
<w:r>
  <w:rPr>
    <w:vertAlign w:val="superscript"/>
    <w:lang w:val="{{footer_lang}}"/>
  </w:rPr>
  <w:t>th</w:t>
</w:r>
<w:r>
  <w:rPr><w:lang w:val="{{footer_lang}}"/></w:rPr>
  <w:t xml:space="preserve"> 2026</w:t>
</w:r>
```

---

## Special character escaping

Escape these three before the text ever reaches `<w:t>`. Order matters — `&` first,
or you double-escape the entities you just wrote.

| Character | XML entity | Example |
|-----------|------------|---------|
| `&` | `&amp;` | `Survey &amp; Topographical Data` |
| `<` | `&lt;` | `depth &lt; 50mm` |
| `>` | `&gt;` | `slope &gt; 1:20` |

Raw `&`, `<` or `>` in `<w:t>` content will corrupt the XML. Always escape.

Never escape a character twice: `&amp;amp;` renders on the page as the literal text
`&amp;`, and validation will not catch it.

Curly quotes and apostrophes are **typography, not escaping** — they need no entity.
If the project calls for them, that rule lives in the style skill's `typography.md`,
not here. The renderer does not make that substitution on its own.

---

## Common XML pitfalls

| Pitfall | Fix |
|---------|-----|
| Leading/trailing space stripped | Add `xml:space="preserve"` to `<w:t>` |
| Raw `&`, `<` or `>` in text | Escape to `&amp;`, `&lt;`, `&gt;` |
| Text escaped twice | Escape once, at the point the text enters the XML |
| New numbering definitions | Never — reference existing `numId="{{num_id}}"` from `numbering.xml` |
| Modifying header/footer files | Never — leave all `header*.xml`, `footer*.xml`, `word/media/` untouched |
| Using npm `docx` library | Never — template clone + XML edit only |
| Person names in Action column | Never — companies/teams only |
| Status values outside the permitted set | Never — `In Progress`, `Pending`, `No Action` only |
