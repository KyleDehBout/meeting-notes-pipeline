# Title Block and Attendees Reference

> **Generated by setup from `{{source_docx}}` on {{rendered_on}}.**
> Values below were extracted from that branded template. To change them, edit this
> file directly — setup will not overwrite it again. To re-derive everything from a
> different .docx, run `setup_docx_renderer.py <new.docx> --force`.

## Contents
- Page setup (sectPr)
- Title block paragraphs
- Attendees label
- Attendee rows

---

## Page setup

Copy `<w:sectPr>` verbatim from the unpacked template. Do not recalculate these
values — they are specific to your branded template file.

```xml
<w:pgSz w:w="{{page_w}}" w:h="{{page_h}}"/>
<w:pgMar w:top="{{mar_top}}" w:right="{{mar_right}}" w:bottom="{{mar_bottom}}" w:left="{{mar_left}}"
         w:header="{{mar_header}}" w:footer="{{mar_footer}}" w:gutter="{{mar_gutter}}"/>
<w:cols w:space="{{cols_space}}"/>
{{title_pg_tag}}
```

`{{mar_top}}` DXA top margin leaves room for the letterhead graphic in the header —
do not reduce it.

---

## Title block

Four paragraphs in order: project name → "Held on" → date → time.

All four are centred, bold, {{title_sz}} half-points, no spacing after, line spacing
{{title_line}} auto.

```xml
<w:p>
  <w:pPr>
    <w:spacing w:after="0" w:line="{{title_line}}" w:lineRule="auto"/>
    <w:jc w:val="center"/>
    <w:rPr>
      <w:b/><w:bCs/>
      <w:sz w:val="{{title_sz}}"/><w:szCs w:val="{{title_sz_cs}}"/>
      <w:lang w:val="{{title_lang}}"/>
    </w:rPr>
  </w:pPr>
  <w:r>
    <w:rPr>
      <w:b/><w:bCs/>
      <w:sz w:val="{{title_sz}}"/><w:szCs w:val="{{title_sz_cs}}"/>
      <w:lang w:val="{{title_lang}}"/>
    </w:rPr>
    <w:t>{{project_name_xml}} - Project Meeting</w:t>
  </w:r>
</w:p>
```

Repeat for: `Held on` / `[the meeting date]` / `[Time]`

**Date format: `{{date_format}}`** — no superscript ordinal suffix in the title block.

This is the project's date convention, set during setup. It is the same rule stated in the
style skill's `references/typography.md`; if the two ever disagree, typography.md is the
source of truth and this file is stale.

The opening empty paragraph (before the title block) uses the same `<w:rPr>` with no
`<w:r>` child.

---

## Attendees label

{{att_font}}, {{att_sz}} half-points, bold, line spacing {{att_line}} auto, no spacing after.

```xml
<w:p>
  <w:pPr>
    <w:spacing w:after="0" w:line="{{att_line}}" w:lineRule="auto"/>
    <w:rPr>
      <w:rFonts w:ascii="{{att_font_xml}}" w:hAnsi="{{att_font_xml}}"/>
      <w:b/><w:bCs/>
      <w:sz w:val="{{att_sz}}"/><w:szCs w:val="{{att_sz_cs}}"/>
      <w:lang w:val="{{att_lang}}"/>
    </w:rPr>
  </w:pPr>
  <w:r>
    <w:rPr>
      <w:rFonts w:ascii="{{att_font_xml}}" w:hAnsi="{{att_font_xml}}"/>
      <w:b/><w:bCs/>
      <w:sz w:val="{{att_sz}}"/><w:szCs w:val="{{att_sz_cs}}"/>
      <w:lang w:val="{{att_lang}}"/>
    </w:rPr>
    <w:t>Attendees:</w:t>
  </w:r>
</w:p>
```

---

## Attendee rows

One paragraph per person. Three tab stops: Name (0) / Company ({{att_tab_company}}) /
Position ({{att_tab_position}}).

```xml
<w:p>
  <w:pPr>
    <w:tabs>
      <w:tab w:val="left" w:pos="0"/>
      <w:tab w:val="left" w:pos="{{att_tab_company}}"/>
      <w:tab w:val="left" w:pos="{{att_tab_position}}"/>
    </w:tabs>
    <w:spacing w:after="0" w:line="{{att_line}}" w:lineRule="auto"/>
    <w:rPr>
      <w:rFonts w:ascii="{{att_font_xml}}" w:hAnsi="{{att_font_xml}}"/>
      <w:sz w:val="{{att_sz}}"/><w:szCs w:val="{{att_sz_cs}}"/>
      <w:lang w:val="{{att_lang}}"/>
    </w:rPr>
  </w:pPr>
  <w:r>
    <w:rPr>
      <w:rFonts w:ascii="{{att_font_xml}}" w:hAnsi="{{att_font_xml}}"/>
      <w:sz w:val="{{att_sz}}"/><w:szCs w:val="{{att_sz_cs}}"/>
      <w:lang w:val="{{att_lang}}"/>
    </w:rPr>
    <w:t>Jane Smith</w:t>
    <w:tab/><w:t>Organisation Name</w:t>
    <w:tab/><w:t>Project Manager</w:t>
  </w:r>
</w:p>
```

Repeat for each attendee. Follow with two empty paragraphs before the table.
